{{ form_start(form) }}
    {{ form_row(form.id, {'attr': {'class': 'form-control', 'value': (category.id is defined)? category.id: ''}}) }}
    <div class="form-group">
        {{ form_row(form.name, {'attr': {'class': 'form-control', 'value': (category.title is defined)? category.title: ''}}) }}
    </div>
    <button class="btn {{ button_color|default('btn-outline-primary') }}  mt-3">{{ button_label|default('Save category') }}</button>
{{ form_end(form) }}